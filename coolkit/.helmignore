# Patterns to ignore when packaging the chart.
.DS_Store
.git/
.gitignore
*.tmp
*.bak
*.swp
*.orig
.vscode/
.idea/
# Docs and ArgoCD manifests live in the repo but are not part of the packaged chart.
../docs/
../argocd/
